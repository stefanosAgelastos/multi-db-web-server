const postToken = require("../routes/token.mysql");


describe('test POST /token router function', () => {

    test('POST: valid input', () => {
        // sample code, now the test just passes
        expect(true).toEqual(true);
    });
})
